{
  "tasks": [
    {
      "name": "style",
      "group": "pre-commit",
      "command": "dotnet",
      "args": ["format", "Haiku.slnx", "style", "--no-restore", "--include", "${staged}"],
      "include": ["src/**/*.cs"]
    },
    {
      "name": "analyzers",
      "group": "pre-commit",
      "command": "dotnet",
      "args": ["format", "Haiku.slnx", "analyzers", "--no-restore", "--include", "${staged}"],
      "include": ["src/**/*.cs"]
    },
    {
      "name": "csharpier",
      "group": "pre-commit",
      "command": "dotnet",
      "args": ["csharpier", "format", "--config-path", "./.csharpierrc.json", "${staged}"],
      "include": ["src/**/*.cs"]
    }
  ]
}
